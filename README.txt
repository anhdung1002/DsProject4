The source code can be complied with Java 8 and higher. 

To compile, use: javac Project3.java

To execute the program, use: java Project2 [input file path] [output file path]

To play multiple games, just re-run the command. Each game has a new shuffled deck.

Note: File path need to include file type. The path can be absolute or relative.